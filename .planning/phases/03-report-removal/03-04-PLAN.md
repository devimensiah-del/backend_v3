# Plan 03-04: Delete Report Domain Package

**Phase:** 03-report-removal
**Plan:** 04 of 05
**Status:** Blocked
**Depends:** 03-03 (no remaining imports)

---

## Objective

Delete the entire domain/report/ directory.

---

## Context

@file:domain/report/ - 11 files to delete

**Files to delete:**
- model.go (~120 lines)
- repository.go (~200 lines)
- service.go (~250 lines)
- async_service.go (~80 lines)
- templating.go (~300 lines)
- theme.go (~50 lines)
- repository_test.go (~150 lines)
- service_test.go (~200 lines)
- templating_test.go (~100 lines)
- analysis_validator_test.go (~100 lines)
- integration_test.go (~150 lines)

**Why this matters:** This is the actual domain removal - eliminating ~1700 lines of dead code.

---

## Tasks

### Task 1: Verify no remaining imports
**Type:** verify
**Files:** All Go files
**Action:**
```bash
grep -r "backend_v3/domain/report" . --include="*.go"
```
Must return nothing (or only this file).

If any imports found, they must be fixed before proceeding.

**Verify:** No imports found

---

### Task 2: Delete report domain directory
**Type:** delete
**Files:** `domain/report/` (entire directory)
**Action:**
```bash
rm -rf domain/report/
```

**Verify:** `ls domain/` shows no report directory

---

### Task 3: Verify build passes
**Type:** verify
**Files:** N/A
**Action:**
```bash
go build .
go build ./...
```

Both must pass with no errors about missing report package.

**Verify:** Build successful

---

### Task 4: Run all tests
**Type:** verify
**Files:** N/A
**Action:**
```bash
go test ./... -v
```

All tests must pass. Report tests are now deleted, so test count will decrease.

**Verify:** All remaining tests pass

---

## Verification

```bash
# Directory deleted
[ ! -d "domain/report" ] && echo "DELETED" || echo "STILL EXISTS"

# Build passes
go build ./...

# Tests pass
go test ./... -v 2>&1 | tail -20

# No lingering references
grep -r "report" ./domain/ --include="*.go" | grep -v "// old report" | grep -v "_test.go"
# Should return nothing related to report domain
```

---

## Success Criteria

- [ ] No imports of domain/report remain
- [ ] domain/report/ directory deleted
- [ ] Full build passes
- [ ] All remaining tests pass
- [ ] ~1700 lines of code removed

---

## Output

Create `03-04-SUMMARY.md` documenting:
- Files deleted (list with line counts)
- Total lines removed
- Test count before/after
- Commit hash
