# Plan 01-02: Framework Database Migration & Seed

**Phase:** 01-foundation
**Plan:** 02 of 04
**Status:** Ready
**Depends:** 01-01 (framework package exists)

---

## Objective

Create database migration for `frameworks` table and seed with the current 11 frameworks.

---

## Context

@file:migrations/028_analyses_pdf_columns.sql - Migration pattern reference
@file:domain/analysis/model.go - Current 11 framework struct definitions
@file:llm/prompts.go - Current prompt templates for each framework

**Why this matters:** The frameworks table stores all framework metadata including prompts, making the system data-driven instead of code-driven.

---

## Tasks

### Task 1: Create frameworks table migration
**Type:** create
**Files:** `migrations/032_create_frameworks_table.sql`
**Action:**
```sql
CREATE TABLE frameworks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    name_pt VARCHAR(100) NOT NULL,
    description TEXT,
    description_pt TEXT,
    category VARCHAR(50) NOT NULL,
    layer_order INTEGER NOT NULL DEFAULT 1,
    is_active BOOLEAN DEFAULT true,
    requires_enrichment BOOLEAN DEFAULT true,
    timeout_seconds INTEGER DEFAULT 60,
    prompt_template TEXT NOT NULL,
    output_schema JSONB NOT NULL,
    preferred_model VARCHAR(50),
    temperature DECIMAL(3,2) DEFAULT 0.7,
    depends_on TEXT[] DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_frameworks_code ON frameworks(code);
CREATE INDEX idx_frameworks_category ON frameworks(category);
CREATE INDEX idx_frameworks_active ON frameworks(is_active) WHERE is_active = true;
```

**Verify:** Review SQL syntax

---

### Task 2: Create seed migration for 11 frameworks
**Type:** create
**Files:** `migrations/033_seed_frameworks.sql`
**Action:**
Insert 11 frameworks with:
- Layer 1 (environment): pestel, porter, tam_sam_som
- Layer 2 (positioning): swot, benchmarking
- Layer 3 (strategy): blue_ocean, growth_hacking, scenarios
- Layer 4 (execution): okrs, bsc, decision_matrix

For each framework:
- Extract prompt_template from llm/prompts.go
- Create output_schema from analysis/model.go struct
- Set appropriate category and layer_order
- Portuguese names (name_pt)

**Verify:** SQL syntax valid

---

### Task 3: Test migration locally
**Type:** verify
**Files:** N/A
**Action:**
```bash
# Connect to database and run migrations
# Or use migration tool if configured
```

**Checkpoint:** human-verify
Confirm migration runs without errors in development database.

---

## Verification

```bash
# Check SQL files exist
ls migrations/032_create_frameworks_table.sql
ls migrations/033_seed_frameworks.sql

# Validate SQL syntax (if psql available)
# psql -f migrations/032_create_frameworks_table.sql --set ON_ERROR_STOP=1
```

---

## Success Criteria

- [ ] `migrations/032_create_frameworks_table.sql` creates table
- [ ] `migrations/033_seed_frameworks.sql` inserts 11 frameworks
- [ ] All 11 frameworks have prompt_template extracted from llm/prompts.go
- [ ] Migrations run successfully on dev database

---

## Output

Create `01-02-SUMMARY.md` documenting:
- Migrations created
- Framework data seeded
- Any prompt adjustments made
- Commit hash
